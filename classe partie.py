import map.py

class Partie:
    nom=""
    id=""
    joueurs = []
    map = None

    def __init__(self):
        pass

    def rejoindre_Partie(self,joueur):
        pass

    def debut_Partie():
        if joueurs.length=4:
            print("narration du jeu")

    def joueur_meurt(joueurs):

        while true:                 #boucle qui tourne pendant toute la partie
            for i in range joueurs.length:
                if joueurs[i].energie=0:
                    print(joueurs[i] "est mort")
                for j in range (i,joueurs.length-1):   #la liste de joueurs s'adapte
                    joueurs[j]=joueurs[j+1]

    def fin_de_partie(joueurs):
        while true:

